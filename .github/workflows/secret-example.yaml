name: Secrets example
on: push
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: |
          echo $NEW_SECRET
        env: 
          NEW_SECRET: ${{ secrets.NEW_SECRET }}